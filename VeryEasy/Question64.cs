namespace Practice.VeryEasy
{
    internal class Question64
    {
        public int[] MultiplyByLength(int[] arr)
        {
            for (int i = 0; i < arr.Length; i++)
            {
                arr[i] *= arr.Length;
            }
            return arr;
        }

        public int[] MultiplyByLength1(int[] arr)
        {
            for (int i = 0; i < arr.Length; i++)
            {
                arr[i] *= arr.Length;
            }
            return arr;
        }

        public int[] MultiplyByLength2(int[] arr)
        {
            for (int i = 0; i < arr.Length; i++)
            {
                arr[i] *= arr.Length;
            }
            return arr;
        }
    }
}