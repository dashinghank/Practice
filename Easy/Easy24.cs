using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Practice.Easy
{
    class Easy24
    {
        public object[] RemoveDups(object[] str)
        {
            return str.Distinct().ToArray();
        }
        public object[] RemoveDups1(object[] str)
        {
            return str.Distinct().ToArray();
        }
        public object[] RemoveDups2(object[] str)
        {
            return str.Distinct().ToArray();
        }
    }
}
