using System;

namespace Practice.Easy
{
    internal class Easy22
    {
        public string AlphabetSoup(string str)
        {
            char[] strarr = str.ToCharArray();
            Array.Sort(strarr);
            return new string(strarr);
        }

        public string AlphabetSoup1(string str)
        {
            char[] strarr = str.ToCharArray();
            Array.Sort(strarr);
            return new string(strarr);
        }

        public string AlphabetSoup2(string str)
        {
            char[] strarr = str.ToCharArray();
            Array.Sort(strarr);
            return new string(strarr);
        }
    }
}